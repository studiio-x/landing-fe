import ChatContainer from "@/components/dashboard/studio/chatbot/ChatContainer";

const ChatbotPage = () => {
  return (
    <div className="flex items-center justify-center w-full h-full">
      <ChatContainer />
    </div>
  );
};

export default ChatbotPage;
